# PRD：Cambly 课后回放「语音转文字」抓取插件 + 复习 Skill

## 1. 背景与目标
用户在 Cambly 课后回放页面（需登录）查看「语音转文字」内容。平台已将语音转写成文字，但缺乏导出能力，导致只能下载视频再转写，成本高且重复。

**目标**：
1) 提供一个 Chrome 插件，抓取「语音转文字」文本并区分老师/学生，保存到本地。
2) 基于本地转写数据，提供一个 OpenClaw/Claude Code/Codex 等可用的 Skill，回答“昨天的课/某天的课我该记哪些短语/单词？”等问题。

---

## 2. 用户画像与核心需求
**用户**：学习者

**核心需求**：
- 登录状态下直接抓取转写文本
- 明确区分老师与学生发言（保证后续只从老师发言中选词）
- 一键保存到本地
- 后续可按日期查询课程重点词/短语，并附上下文
- 推荐难度与用户水平匹配：可稍难但不能过简单

---

## 3. 项目范围（Scope）
### 3.1 Chrome 插件（MVP）
1. **页面登录检测**
   - 插件仅在用户已登录并进入回放详情页时工作。

2. **文本抓取**
   - 抓取「语音转文字」tab 内所有文本条目。

3. **说话人识别（支持用户配置）**
   - 插件提供设置项：让用户输入自己的名字 → 作为学生名。
   - 另一方自动归为老师名（或可额外填写老师名）。
   - 适配老师名字不含“Teach”等关键词的情况。

4. **时间戳抓取**
   - 为每条文本抓取对应时间点（分:秒），方便回看视频定位。
   - 若页面提供时间数据则直接读取；如无则按 DOM 节点关联视频时间轴。

5. **本地保存**
   - 插件仅做抓取并保存到本地（不负责下游处理）。
   - 允许用户选择本地文件夹路径；未选择时默认保存到“下载”目录。
   - 保存格式建议：JSON + Markdown（同时生成便于检索与阅读的版本）。

6. **导出/复制**
   - 一键复制或导出文本。

### 3.2 OpenClaw Skill（MVP）
1. **数据输入**
   - 读取插件保存的本地文本（按日期归档）。

2. **问题类型（灵活）**
   - 支持多种自然语言提问，如：
     - “昨天的课我该记哪些短语/单词？”
     - “我想复习一下 xxx 天 Cambly 的课”
     - “帮我回顾一下某天课程的重点表达”

3. **结果输出**
   - 输出推荐短语/单词列表
   - 每条需包含：
     - 短语/单词
     - 老师当时使用的完整句子（英文）
     - 出现的时间戳（分:秒）便于回看视频
     - 对话话题/上下文（英文呈现）
     - 2–3 个额外英文例句

4. **关键约束**
   - 只从**老师发言**中抽取（学生发言不作为候选）。
   - 先整体评估用户英语水平，再匹配合适难度（可略难，避免过简单）。

---

## 4. 用户流程
### 4.1 插件使用流程
1. 用户登录 Cambly 并打开课后回放页面
2. 点击「语音转文字」tab
3. 设置中输入“学生名字”
4. 点击插件按钮 → 自动抓取并保存

### 4.2 Skill 使用流程
1. 用户提问：昨天的课/某天的课我该记哪些短语/单词？
2. Skill 读取对应日期的对话记录
3. 分析英语水平 + 抽取老师发言中的词/短语
4. 输出词/短语 + 原句 + 话题上下文

---

## 5. 输出格式示例
**插件输出（Markdown）**
```
[Teacher] Wow. That is pretty cool.
[Student] emperor, yeah, for one emperor...
```

**Skill 输出**
- phrase: **magnificent building**
  - sentence: "It was just such a magnificent building at the time."
  - timestamp: 12:34
  - context: Discussing the size and history of the Colosseum.
  - examples:
    - "The cathedral is a magnificent building with stained glass windows."
    - "They preserved the magnificent building as a national landmark."
    - "She studied the magnificent building for her architecture class."

---

## 6. 风险与边界
- 页面 DOM 可能变更 → 需可配置选择器或容错策略
- 教师姓名可能多样 → 必须支持用户配置
- 懒加载/滚动加载 → 需处理滚动抓取

---

## 7. 成功指标
- 文本抓取成功率 ≥ 95%
- 说话人区分准确率 ≥ 95%
- 用户可直接从 Skill 获得可复习的词/短语清单

---

## 8. 非目标
- 不做语音转写引擎
- 不绕过登录或权限
- 不在插件内做分析（分析交给 Skill）
